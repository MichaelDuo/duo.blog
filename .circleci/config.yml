version: 2
jobs:
  build-deploy:
    docker:
      - image: circleci/node:10.13.0
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Install packages
          command: npm install
      - run:
          name: Build files
          command: npm run build
      - run:
          name: Config git
          command: |
            git config --global user.email "michaeldong21@gmail.com" # TODO: Move to env variable
            git config --global user.name "Bot" # TODO: Move to env variable
      - run:
          name: Deploy
          command: node scripts/deploy
workflows:
  version: 2
  build-deploy:
    jobs:
      - build-deploy

# S3 deployment config
# version: 2
# jobs:
#   build:
#     docker:
#       - image: circleci/node:10.13.0
#     steps:
#       - checkout
#       - setup_remote_docker
#       - run:
#           name: Install packages
#           command: npm install
#       - run:
#           name: Build files
#           command: npm run build
#       - persist_to_workspace:
#           root: .
#           paths:
#             - build
#   deploy:
#     docker:
#       - image: circleci/python:3.6.1
#     environment:
#       AWS_DEFAULT_OUTPUT: json
#     steps:
#       - attach_workspace:
#           at: workspace
#       - run:
#           name: Install awscli
#           command: |
#             python3 -m venv venv
#             . venv/bin/activate
#             pip install awscli
#       - run:
#           name: Upload files
#           command: |
#             . venv/bin/activate
#             aws s3 sync workspace/build s3://$BUCKET_NAME --delete
# workflows:
#   version: 2
#   build-deploy:
#     jobs:
#       - build
#       - deploy:
#           requires:
#             - build
#           filters:
#             branches:
#               only: master